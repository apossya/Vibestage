Создай **веб-приложение (Node.js + Socket.io)** с названием **«VibeStage»**.

---

## ОПИСАНИЕ ПРОЕКТА:

VibeStage превращает обычную демо-сессию в интерактивное шоу: зрители сканируют QR-код, получают «пульт» на телефоне и в реальном времени шлют эмодзи-реакции, голосуют и наблюдают таймер. После каждого питча сервис автоматически выдаёт команд-победителей смешные GIF-кубки, сгенерированные на лету.

## ФУНКЦИОНАЛ:

* **Таймер-джем** – на странице «host» запускается 5-минутный обратный отсчёт с резким «Time’s up» и конфетти.
* **Live-реакции** – кнопки 🔥 🤯 😂 💡 на телефонах моментально летят на экран с анимацией parallax.
* **Опрос + GIF-трофеи** – после доклада появляется блиц-опрос; по итогам лайков сервис стягивает 3 случайных GIF из Giphy, пишет текст категории на Canvas и показывает победный ролик.

## ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:

* **Технологии:** Node.js, Express, Socket.io (web-sockets), node-canvas, node-fetch, QRCode.js, Vanilla JS, HTML, CSS (Tailwind CDN или собственный минимал).
* **Платформа:** веб-приложение для Replit.
* **Дизайн:** минималистичный, акцент на эмодзи-анимациях и крупном таймере.
* **Адаптивность:** полностью работает на мобильных устройствах (remote.html) и на большом экране (host.html).

## СТРУКТУРА:

```
.
├── index.js          # Express + Socket.io сервер
├── gif.js            # makeTrophy(category) – Giphy + Canvas
├── public/
│   ├── host.html     # Экран проектора
│   ├── remote.html   # Смарт-пульт для телефонов
│   ├── style.css     # Tailwind CDN или кастом
│   └── qrcode.js     # Библиотека генерации QR
└── package.json      # зависимости
```

* Комментарии в каждом файле объясняют логику.
* Код разбит на функции (таймер, реакции, опрос, генератор GIF) для удобной доработки.

## ОСОБЫЕ ПОЖЕЛАНИЯ:

* На главной странице (`/`) сразу показывай QR-код, ведущий на `/remote`.
* Добавь «секретный» cheat-код ↑↑↓↓←→←→BA в `remote.html`, который вызывает глобальный взрыв конфетти.
* При генерации GIF-трофея сохраняй mp4/APNG в `/public/trophies` и авто-показывай на host-экране.
* UI может переключаться между RU/EN (двуязычные надписи).

---

Начни с базовой версии:

1. Сервер Express + Socket.io, отдающий `host.html`, `remote.html` и статику.
2. Реализуй двусторонний канал `react`, `timer`, `poll` (минимальные события).
3. Добавь QR-код генератор на `/` и тестовую кнопку эмодзи на `remote.html`.

Эту основу уже можно запустить и протестировать; затем постепенно добавляй анимации, опрос и генератор GIF-кубков.
